project:
  version: effnet_ssl_clean

model:
  pretrained: false
  use_mixstyle: false

train:
  head_epochs: 3
  finetune_epochs: 15
  loss: bce
  pretrained_backbone_path: artifacts/checkpoints/ssl/effnet_simclr_clean.pth

infer:
  checkpoint_path: artifacts/checkpoints/effnet_ssl_clean/best.pt
