project:
  version: v6_gamma_only

train:
  loss: focal
  focal_gamma: 2.0
  head_epochs: 0
  finetune_epochs: 15
  backbone_learning_rate: 0.00004
  pretrained_backbone_path: artifacts/runs/01_ssl_lineage/v4.1/checkpoints/best.pt

infer:
  checkpoint_path: artifacts/runs/01_ssl_lineage/v6_gamma_only/checkpoints/best.pt
